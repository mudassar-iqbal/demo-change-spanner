server.port: 9000

spring:
 profiles.active: dev
 cloud:
   gcp:
     project-id: iconic-scholar-307206
     # credentials.location: file:C:\\Users\\ashu3\\AppData\\Roaming\\gcloud\\application_default_credentials.json
     spanner:
       instance-id: spannerpocinstance
       database: pocdb

poller:
 commit:
  table:
   name: POLLER_COMMIT_TIMESTAMPS
project:
 name: gcp-practice-project-307011
primary:
 table:
   name: pnr

## logging
logging:
  pattern.console: '%d{yyyy-MM-dd HH:mm:ss} [%X{source}] [%X{correlation-id}] [%-5level] [%C:%L] - %m%n'
  file: logs/spanner-pollar-convereter-publisher/app.log
  level.org.springframework: info

# actuator properties
management:
  endpoints.web.exposure.include: '*'
  endpoint.health.show-details: always
  info.git.mode: full

info:
  app:
    name: demo-change-spanner
    description: Google cloud spanner 
    version: 1.0.0
    java-vendor: ${java.specification.vendor}

app:
  topic.name: spannertopic
---
spring.profiles: dev

---
spring.profiles: test

---
spring.profiles: prod